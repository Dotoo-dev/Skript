on join:
	if {firstjoinday%player%} is not set:
		set {firstjoinday%player%} to now
on quit:
	set {lastleave%player%} to now
every 1 minutes:
	add 1 to {playedtimer}
	if {playedtimer} is 20:
		loop all players:
			add 20 to {playedminutes%loop-player%}
		set {playedtimer} to 0
